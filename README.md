# Python Trial

A simple Python project for testing Git, GitHub, and virtual environments.

## How to run
1. Activate the virtual environment:
2. Run the script:

## Project Structure
- `main.py` — main Python script  
- `venv/` — local virtual environment (ignored by Git)
